# PR 1.5 Start Prompt — Debt Seed And Guard Hardening

Copy this prompt into the next agent session to start PR 1.5.

```md
We are working in `/Users/linussteen/Documents/SteenBudgetSolution`.

Implement **PR 1.5 — Debt Seed And Guard Hardening** only.

Primary brief:

- `Work/Dashboard/Debt/PR-01.5-be-debt-seed-and-guard-hardening.md`

## Hard Rules

- Do not implement PR 2 create/edit metadata endpoints.
- Do not add balance history.
- Do not add paid-off/archive/restore/skip/remove endpoints.
- Do not build frontend UI.
- Do not create production migration or ALTER scripts. This app has no
  production debt data yet; update init schema and seeds directly.
- Do not commit or push.
- Keep planned payment, balance, source lifecycle, and month participation
  separate.
- Closed/skipped budget months must remain immutable.

## Scope

Fix the PR 1 hardening gap before any new Debt features are built.

Required outcomes:

1. **Seed/schema reset cleanup**
   - Remove remaining test/init seed use of legacy `Debt.Status = 'closed'`.
   - Use `paidOff`, `archived`, or `deleted` only when the fixture is proving
     that lifecycle.
   - Explicitly seed `BudgetMonthDebt.ParticipationStatus` anywhere a test
     depends on participation.
   - Keep legacy `BudgetMonthDebt.Status` and `IsDeleted` internally consistent
     where they still exist, but do not make new logic depend on them.

2. **Wizard/onboarding verification**
   - Trace the wizard debt path from saved wizard debt data through
     finalization and materialization.
   - Confirm wizard-created source debts are `Debt.Status = 'active'`.
   - Confirm materialized month debts from wizard-created debts are open,
     visible rows:
     - `BudgetMonthDebt.ParticipationStatus = 'included'`
     - legacy `BudgetMonthDebt.Status = 'active'`
     - `BudgetMonthDebt.IsDeleted = 0`
   - Add or adjust integration coverage for this. Do not assume it works just
     because source materialization tests pass.

3. **Single vs bulk mutation parity**
   - Single and bulk planned-payment patch must reject the same invalid states.
   - Bulk patch must reject:
     - `ParticipationStatus = 'removed'`
     - `ParticipationStatus = 'notIncluded'`
     - source lifecycle `paidOff`
     - source lifecycle `archived`
     - source lifecycle `deleted`
   - Preserve bulk all-or-nothing behavior: if one row is invalid, none of the
     requested rows are changed and no partial audit is written.
   - Prefer a shared guard/helper over duplicating conditionals in two handlers.

4. **Default editor read filtering**
   - Normal Debt editor reads must hide `ParticipationStatus = 'removed'`.
   - This must work even when legacy `IsDeleted = 0`.
   - Add a regression test where `ParticipationStatus = 'removed'` and
     `IsDeleted = 0`.

## Read First

Project rules:

- `AGENTS.md`
- `PROJECT.md`
- `.agents/instructions/backend.instructions.md`

Debt planning:

- `Work/Dashboard/Debt/README.md`
- `Work/Dashboard/Debt/IMPLEMENTATION-PLAN.md`
- `Work/Dashboard/Debt/PR-00-source-analysis.md`
- `Work/Dashboard/Debt/PR-01-be-debt-lifecycle-participation-model.md`
- `Work/Dashboard/Debt/PR-01.5-be-debt-seed-and-guard-hardening.md`
- `Work/Dashboard/Debt/PR-02-be-debt-create-edit-metadata.md`

Schema and seeds:

- `database/init/01-full-schema.sql`
- `database/init/04-MonthlyLifeCycle.sql`
- `tests/Backend.IntegrationTests/Shared/Seeds/Budget/DbSeeds.cs`
- `tests/Backend.IntegrationTests/Shared/Seeds/Budget/BudgetSeeds.cs`
- `tests/Backend.IntegrationTests/Shared/Seeds/Wizard/WizardSeeds.cs`

Lifecycle/participation constants and models:

- `Backend/Application/Constants/DebtSourceLifecycleStatuses.cs`
- `Backend/Application/Constants/BudgetMonthDebtParticipationStatuses.cs`
- `Backend/Application/Features/Budgets/Months/Editor/Models/Debts/BudgetMonthDebtMutationReadModel.cs`
- `Backend/Application/Features/Budgets/Months/Editor/Models/Debts/BudgetMonthDebtEditorRowReadModel.cs`

Debt editor mutation path:

- `Backend/Application/Features/Budgets/Months/Editor/Debts/PatchDebt/PatchBudgetMonthDebtCommand.cs`
- `Backend/Application/Features/Budgets/Months/Editor/Debts/PatchDebt/PatchBudgetMonthDebtCommandHandler.cs`
- `Backend/Application/Features/Budgets/Months/Editor/Debts/PatchDebt/PatchBudgetMonthDebtCommandValidator.cs`
- `Backend/Application/Features/Budgets/Months/Editor/Debts/PatchDebtsBulk/PatchBudgetMonthDebtsBulkCommand.cs`
- `Backend/Application/Features/Budgets/Months/Editor/Debts/PatchDebtsBulk/PatchBudgetMonthDebtsBulkCommandHandler.cs`
- `Backend/Application/Features/Budgets/Months/Editor/Debts/PatchDebtsBulk/PatchBudgetMonthDebtsBulkCommandValidator.cs`
- `Backend/Application/Abstractions/Infrastructure/Data/IBudgetMonthDebtMutationRepository.cs`
- `Backend/Infrastructure/Repositories/Budget/Months/Editor/Debts/BudgetMonthDebtMutationRepository.cs`
- `Backend/Infrastructure/Repositories/Budget/Months/Editor/Debts/BudgetMonthDebtMutationRepository.Sql.cs`
- `Backend/Domain/Errors/Budget/Errors.BudgetMonthDebtErrors.cs`

Wizard and materialization path:

- `Backend/Application/Mappings/Budget/DebtMapping.cs`
- `Backend/Application/Features/Wizard/Finalization/FinalizeWizardCommandHandler.cs`
- `Backend/Application/Features/Wizard/Finalization/Processing/Processors/DebtStepProcessor.cs`
- `Backend/Application/Features/Wizard/Finalization/Materialize/WizardBudgetMaterializer.cs`
- `Backend/Application/Features/Wizard/Finalization/Targets/FinalizeBudgetTarget.cs`
- `Backend/Application/Features/Wizard/Finalization/Targets/FinalizeBudgetTargetFactory.cs`
- `Backend/Application/Features/Wizard/Finalization/Abstractions/IDebtTarget.cs`
- `Backend/Application/Features/Wizard/Finalization/Abstractions/IWizardStepOrchestrator.cs`
- `Backend/Application/Services/Budget/Materializer/BudgetMonthMaterializer.cs`
- `Backend/Application/Abstractions/Infrastructure/Data/IBudgetMonthSeedSourceRepository.cs`
- `Backend/Application/Abstractions/Infrastructure/Data/IBudgetMonthMaterializationRepository.cs`
- `Backend/Infrastructure/Repositories/Budget/Months/Seed/BudgetMonthSeedSourceRepository.cs`
- `Backend/Infrastructure/Repositories/Budget/Months/Seed/BudgetMonthSeedSourceRepository.sql.cs`
- `Backend/Infrastructure/Repositories/Budget/Months/Materializer/BudgetMonthMaterializationRepository.cs`
- `Backend/Infrastructure/Repositories/Budget/Months/Materializer/BudgetMonthMaterializationRepository.Sql.cs`

Relevant tests:

- `tests/Backend.IntegrationTests/Budget/BudgetMonths/Editor/BudgetMonthDebtEditorTests.cs`
- `tests/Backend.IntegrationTests/Budget/BudgetMonths/Editor/BudgetMonthDebtLifecycleParticipationTests.cs`
- `tests/Backend.IntegrationTests/Budget/BudgetMonths/BudgetMonthLifecycleMaterializationTests.cs`
- `tests/Backend.IntegrationTests/Wizard/FinalizeWizardFlowTests.cs`
- `tests/Backend.UnitTests/Features/Wizard/WizardFinalizationOrchestratorTests.cs`

Dashboard/month-close regression context, if SQL filters are touched:

- `Backend/Infrastructure/Repositories/Budget/BudgetDashboard/BudgetMonthDashboardRepository.sql.cs`
- `Backend/Infrastructure/Repositories/Budget/Months/BudgetMonthRepository.Sql.cs`
- `tests/Backend.IntegrationTests/Budget/Dashboard/BudgetDashboardMonthQueryHandlerTests.cs`
- month-close integration tests found with:
  `rg -n "CloseBudgetMonth|SnapshotTotalDebtPaymentsMonthly" tests/Backend.IntegrationTests`

## Implementation Notes

- Start with `rg -n "Status\\s*=\\s*'closed'|Status,.*closed|ParticipationStatus|IsDeleted" database Backend tests`.
- Then inspect the bulk handler and single handler side by side.
- If you introduce a shared guard, keep it internal to the Debt editor mutation
  slice unless an existing project pattern says otherwise.
- The precise removed-row error should win over legacy deleted-row behavior
  when both `ParticipationStatus = 'removed'` and `IsDeleted = 1`.
- Do not change dashboard/month-close semantics except where tests expose a
  real PR 1.5 regression.

## Acceptance Criteria

- No init/test seed inserts `Debt.Status = 'closed'`.
- Wizard finalization creates source debts with `Debt.Status = 'active'`.
- Wizard-created source debts materialize into included, active, not-deleted
  `BudgetMonthDebt` rows.
- Bulk planned-payment patch rejects `removed`, `notIncluded`, `paidOff`,
  `archived`, and `deleted`.
- Bulk planned-payment patch remains all-or-nothing.
- Default editor reads hide `ParticipationStatus = 'removed'` even when
  `IsDeleted = 0`.
- Existing PR 1 dashboard/month-close/recap behavior still passes.

## Validation

Run targeted validation, not a blind full-suite sweep:

```bash
dotnet build Backend/Backend.csproj
dotnet test tests/Backend.IntegrationTests --filter BudgetMonthDebtEditorTests
dotnet test tests/Backend.IntegrationTests --filter BudgetMonthDebtLifecycleParticipationTests
dotnet test tests/Backend.IntegrationTests --filter BudgetMonthLifecycleMaterializationTests
dotnet test tests/Backend.IntegrationTests --filter FinalizeWizardFlowTests
dotnet test tests/Backend.IntegrationTests --filter BudgetMonthDashboard
dotnet test tests/Backend.IntegrationTests --filter CloseBudgetMonth
```

If one filter does not match the actual test class name, find the correct class
with `rg` and run that targeted suite instead.

## Completion

When done:

1. Append a short entry to `docs/ai/ai-changelog.md`.
2. Write `COMMIT_MSG.tmp`.
3. Stop.
4. Do not commit or push.
```
