# ───── Runtime mode ───────────────────────────────────────────────
ASPNETCORE_ENVIRONMENT=Production

# ───── Core app secrets ───────────────────────────────────────────
# 32+ random bytes (base64).
WEBSOCKET_SECRET=PASTE_REAL_KEY_HERE
JWT_SECRET_KEY=base64:PASTE_REAL_KEY_HERE
RECAPTCHA_SECRET_KEY=PASTE_REAL_KEY_HERE

# ───── DB ─────────────────────────────────────
DATABASESETTINGS__CONNECTIONSTRING=Server=mariadb;Port=3306;Database=steenbudgetPROD;User=steenbudget;Password=PASTE_REAL_KEY_HERE;Connection Timeout=3;Default Command Timeout=3;GuidFormat=Binary16;OldGuids=false;TreatTinyAsBoolean=false;Pooling=true;Minimum Pool Size=0;Maximum Pool Size=50;SslMode=None
# ───── DB (used by the mariadb container) ─────────────────────────
MARIADB_ROOT_PASSWORD=PASTE_REAL_KEY_HERE
MARIADB_USER=steenbudget
MARIADB_PASSWORD=PASTE_REAL_KEY_HERE
MARIADB_DATABASE=steenbudgetPROD

# ----EMAIL ----
#SMTP (prod only; I leave empty until we pick a provider) ────
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_PASSWORD_INFO=
SMTP_FROM=noreply@ebudget.se

# ───── Infra / reverse proxy ──────────────────────────────────────
CLOUDFLARE_API_TOKEN=PASTE_REAL_KEY_HERE   # for Caddy DNS-01

# ----- Email
AppUrls__VerifyUrl=https://ebudget.se/verify-email